编译：
1. 打开终端切换到工作空间根目录
2. 运行 `catkin_make`（或 `catkin build`）完成 `obstacle_mapping` 包及 `multi_robot_mapping.cpp` 节点的编译。
3.  `source devel/setup.bash` 

启动：
 `roslaunch obstacle_mapping multi_robot_mapping.launch`
